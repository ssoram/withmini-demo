# withmini Demo Mode — 파이프라인 진행 상황

> 이 파일은 모든 서브에이전트와 오케스트레이터가 공유하는 상태 파일이다.
> 상태값: `TODO` → `IN_PROGRESS` → `IN_REVIEW` → `APPROVED` (또는 `CHANGES_REQUESTED`로 반려 후 재작업)
> 각 서브에이전트는 작업 시작/종료 시 반드시 이 표를 갱신한다.

| task | owner | depends_on | status | notes |
|---|---|---|---|---|
| project-setup | architect | - | APPROVED | QA 통과: 6개 테이블 타입/마이그레이션이 스펙 섹션2와 1:1 일치, 전 테이블 RLS 활성화 확인, Storage 5개 버킷(프레임 public read / 세션 private) 확인, anon key만 클라이언트 노출(service_role 미노출) 확인, 3개 앱 라우팅 스펙대로 스캐폴딩 확인, vercel.json+deployment.md 3도메인 분리 확인, npm run build 3개 앱 모두 성공(재확인). 세션 버킷 baseline anon read/write는 마이그레이션 주석에 infra-setup에서 강화 예정으로 명시되어 있어 반려 사유 아님(infra-setup에서 처리 예정). [추가 정정] `packages/shared/src/types.ts`의 `Database.public.Tables.*`에 `Relationships: []`, `public` 스키마에 `Views`/`Functions` 빈 타입을 추가(postgrest-js GenericSchema 요구사항 충족용, 아래 "사람에게 보고가 필요한 항목" 참고). DB 컬럼/타입 정의는 무변경 — 순수 타입 레벨 정정이라 스펙 문서(섹션2) 수정 불필요. apps/booth, apps/result는 `npm run build` 재확인 통과(booth 693 modules, result 정상). apps/admin은 이 정정으로 `.update()`/`.insert()` 인자가 더 이상 `never`가 아니게 되면서 `src/routes/Frames.tsx`에 있던 기존 타입 오류가 새로 드러남(아래 admin-page 행 참고) — types.ts 자체의 문제는 아님 |
| booth-flow | booth-builder | project-setup | APPROVED | QA 통과: 4.1(idle 60초 전화면 공통, 촬영/업로드 중 일시정지) 4.2(is_visible+display_order 카드형) 4.3(is_active+is_general 조회, slot_count 전달) 4.4(getUserMedia, 3-2-1 카운트다운x8, n/8+도트 진행표시, 메모리보관 후 일괄업로드) 4.5(slot_count 정확히 제한, 선택순서=슬롯순서, 완료 시 다음버튼 활성화) 4.6(compositor.ts Canvas 합성, session-raw/session-results 업로드, sessions row 섹션2 컬럼대로 생성/갱신, /print-animation navigate state를 PrintAnimation.tsx가 최우선 소비함을 코드로 확인) 모두 확인. UI는 4.0 원칙(큰 터치타겟, 공통 Screen 레이아웃, 도트 인디케이터 등 장식 최소화) 부합. 계약 파일 확인: supabaseClient.ts·supabase/migrations 0001~0002 내용 불변. packages/shared/src/types.ts는 검토 도중 architect의 Relationships/Views/Functions 정정(project-setup 행 참고)이 반영되는 걸 확인했고, 정정 반영 후 apps/booth npm run build 재실행도 성공 — booth-builder가 임의로 건드린 흔적 없음(Generate.tsx의 as any 우회는 이제 불필요해졌지만 있어도 컴파일/동작에 문제 없어 반려 사유 아님, 후속 정리 권장). PrintAnimation.tsx/Qr.tsx는 라우팅 연결만 되고 내용 미수정. as any 우회 및 signed URL 대신 storage 경로 저장 방식은 team-lead 사전 안내대로 반려 사유 아님으로 처리. 후속 정리: as any 우회 제거 완료. UI 개선(사용자 요청, architect의 0006_concept_images 계약 반영): Start.tsx에 WITHMINI 브랜드 로고 타이포 표시+안내 문구 변경, Concept.tsx는 concepts.image_url(concept-images 버킷)을 카드 배경으로 꽉 채우고 중앙에 컨셉명 오버레이(반투명 어두운 오버레이, image_url 없으면 기존처럼 텍스트만), Frame.tsx는 preview_image_url을 체커보드 배경 위에 크게 보여주고 이름/슬롯수는 하단 보조 텍스트로 변경, Capture.tsx는 getUserMedia 실패 시 NotAllowedError→권한 안내, mediaDevices 미지원/비보안 컨텍스트→HTTPS 안내로 원인별 안내 문구 추가. npm run build 재확인 성공. 긴급 버그 수정: Generate.tsx 1단계의 sessions upsert가 0005_sessions_select_hardening 이후 anon SELECT 정책 부재로 ON CONFLICT 판정(upsert 내부 동작)에서 RLS 42501로 실패하던 문제를, INSERT 우선 시도 후 23505(unique violation, 재시도로 동일 sessionId 존재)일 때만 UPDATE로 대체하는 방식으로 수정(정책 변경 없이 코드로 해결). npm run build 재확인 성공. 긴급 버그 수정 2: lib/storage.ts의 uploadBlob이 upsert:true(x-upsert 업로드)로 세션 버킷에 올리던 것도 같은 이유(0003_infra_hardening으로 anon SELECT 없음)로 RLS 403이 나던 문제를, upsert 없는 일반 업로드로 바꾸고 재시도로 인한 409(Duplicate/"already exists")는 성공으로 간주해 무시하도록 수정. npm run build 재확인 성공. 긴급 버그 수정 3: 0005 이후 anon UPDATE도 WHERE절 SELECT 요구로 조용히 0건 no-op(에러 없이 실패)해 모든 세션이 completed로 전이되지 않던 문제를, architect의 security definer RPC(complete_session, 0007_complete_session_rpc.sql)를 supabase.rpc()로 호출하는 방식으로 교체(4단계). 1단계 23505 재시도 경로의 status 리셋 update도 제거(같은 이유로 no-op이었고 이제 해당 update 정책 자체가 super_admin 전용으로 좁혀짐 — 23505면 그냥 성공 간주). rpc() 호출부만 Database.Functions 빈 타입 문제로 (supabase as any).rpc(...) 로컬 캐스팅(types.ts 미수정, architect 후속 검토 예정). npm run build 재확인 성공 |
| print-animation | animation-builder | project-setup | APPROVED | QA 통과. PrintBoothScene: 슬롯 슬라이드아웃(easeOutCubic)→안착(easeOutBack)→reveal(텍스처 opacity 0→1, easeInOutSine) 연출과 상태표시등(주황→초록) 확인, 스펙4.7 연출 순서 일치. TIMELINE.holdEnd=7.4s로 5~8초 범위 준수, 스킵 버튼 코드 없음(grep 확인). onComplete는 t>=TOTAL_DURATION 시 1회만 호출, PrintAnimation.tsx의 handleAnimationComplete가 navigate("/qr", {state:{...state, resultImageUrl}})로 자동 전환 확인. 결과 이미지 해석 순서(state.resultImageUrl→state.sessionId로 sessions.result_image_url 조회→PLACEHOLDER) 코드상 정확히 해당 순서로 구현됨. packages/shared/src/types.ts, supabase/migrations/*.sql 내용/mtime 모두 이전 검증 시점과 동일해 미수정 확인(postgrest 타입 불일치는 PrintAnimation.tsx 로컬 캐스팅으로만 우회, 공용 타입 변경 없음). apps/booth npm run build 재실행 성공(693 modules, three.js 청크 1.2MB 경고는 빌드 실패 아니므로 반려 사유 아님) | [후속 보안 강화 반영] `PrintAnimation.tsx`의 폴백 조회(sessions anon select)를 `@withmini/shared`의 `fetchSessionMedia`(get-session-media Edge Function, signed URL)로 교체, `status !== 'completed'` 시 기존 PLACEHOLDER 폴백 유지, state.resultImageUrl 우선 경로/연출 타이밍 무변경. apps/booth npm run build 재확인 성공(695 modules) |
| admin-page | admin-builder | project-setup | APPROVED | QA 통과: 5.1(Supabase Auth 이메일/비밀번호 로그인, /login 제외 전 라우트 RequireAuth로 보호, admins.role/is_active 조회 후 비활성 계정 즉시 signOut+에러 메시지, canWrite=super_admin으로 프론트 버튼 제한 — 0001_init.sql의 concepts/frames/settings/admins insert·update·delete RLS 정책이 모두 is_super_admin()으로 동일하게 강제되는 것도 대조 확인) 5.2(목록/추가/이름수정/노출토글/2단계 삭제: is_visible on상태에서 삭제누르면 우선 숨김만, 다시 눌러야 실제 hard delete) 5.3(컨셉필터, 등록/수정/삭제, is_active·is_general 토글, 소속컨셉/slot_count, frame-previews 업로드, layout_data JSON 입력폼) 5.3 추가확인: booth-builder의 compositor.ts 규약(투명 PNG 템플릿 위에 사진을 먼저 그리고 프레임을 덮어씌움, 좌표 0~1=비율/그외=절대픽셀)이 Frames.tsx 폼의 안내문구(hint)에 정확히 반영됨 — pipeline_status.md에 남겨진 조율 요청이 실제로 해소됨. 5.4(최우선 원칙): Sessions.tsx 전수 확인 결과 raw_photo_urls/selected_photo_urls/result_image_url/timelapse_video_url/qr_url을 select하거나 렌더링하는 코드 경로가 전혀 없음 — 통계 쿼리는 concept_id/frame_id/status/created_at만 select, 세션 수동삭제도 storage.list()로 파일명만 확인 후 삭제(내용 미표시). 총 세션수/상태별/기간별/컨셉·프레임별 현황, retention_hours 24~48 범위 검증(RETENTION_MIN/MAX) 확인. 감사로그: concept.create/update/delete, frame.create/update/delete, settings.update, session.delete 모두 admin_audit_logs 기록 확인. 4.0 원칙: index.css가 목록(data-table)/폼(modal-form) 중심의 실용적 UI, 과한 장식 없음. 보안: service_role 키 미사용(anon만 사용), frame-previews/frame-videos 버킷 insert/update/delete RLS도 is_super_admin() 확인. 계약 파일: admin-builder는 types.ts를 직접 수정하지 않고 로컬 db.ts(Prettify 보정 클라이언트)로만 우회 — 검증 도중 architect가 types.ts의 Row/Insert/Update를 interface→type으로 정정하는 걸 실시간으로 확인했으나(DB 컬럼/구조 무변경, admin-page notes에 남긴 요청사항과 일치하는 후속 조치), admin-builder 소행이 아니며 admin build는 정정 전후 모두 성공. npm run build(apps/admin) 두 차례 성공(90 modules). 생략: /staff 라우트는 스펙상 MVP 후순위 허용 항목이라 미구현 — notes에 명시됨. db.ts의 Prettify 로컬 우회는 team-lead 사전 안내대로 반려 사유 아님으로 처리. 후속 정리: architect의 interface→type 정정 이후 격리 테스트(select/insert().select().single()/update() 모두 정상 타입 추론 확인)로 로컬 우회가 더 이상 불필요함을 재확인, `apps/admin/src/lib/db.ts` 삭제 후 전 파일이 공용 `supabase` 클라이언트/타입을 직접 사용하도록 정리(기능 변경 없음), npm run build 재확인 성공(89 modules). 기능 추가(사용자 요청, 스펙 5.3 고도화): 프레임 등록/수정 폼에 layout_data 비주얼 슬롯 에디터 추가(`apps/admin/src/components/SlotEditor.tsx`) — 미리보기 이미지 위에 "칸 추가" 버튼으로 슬롯 생성, 마우스 드래그로 이동/모서리 핸들로 크기조절/×버튼으로 삭제, 순번(1,2,3…)이 사진 선택 순서와 매칭됨을 안내, 저장 시 이미지 natural 크기 대비 0~1 비율로 정규화, slot_count는 슬롯 개수와 자동 동기화(수동 입력 제거), 기존 프레임 수정 시 저장된 slots(비율/레거시 절대픽셀 모두 지원, 이미지 로드 시 자동 변환) 복원 표시, 기존 JSON 직접 입력은 `<details>` 고급 편집 영역으로 유지하며 양방향 동기화(파싱 실패 시 에러 표시, 슬롯 배열 미반영). 외부 라이브러리 미사용(순수 마우스 이벤트), 계약 파일 무변경. npm run build 재확인 성공(91 modules). 기능 추가(사용자 요청): SlotEditor에 "슬롯 자동 인식" 버튼 추가(`apps/admin/src/lib/slotDetection.ts`) — 프레임 PNG를 오프스크린 canvas(긴 변 1000px 초과 시 다운스케일)에 그려 알파<25 픽셀을 4방향 BFS(명시적 스택)로 연결요소 묶음, 전체 면적 1% 미만 노이즈 제외, 가장자리 접촉 영역은 기본 제외하되 결과 0개면 가장자리 포함 재시도, 바운딩박스를 0~1 비율로 변환 후 위→아래/같은 줄(y중심 차이<=높이/2)이면 왼쪽→오른쪽 순서로 정렬. 기존 슬롯 있으면 confirm 후 교체, 인식 결과 개수 안내("N개 칸을 찾았습니다"/"찾지 못했습니다"), 실패(CORS 등) 시 에러 메시지만 표시하고 기존 슬롯 유지. compositor.ts의 loadImage와 동일하게 crossOrigin='anonymous'로 로드해 기존 프레임 수정(Storage 공개 URL) 화면에서도 동작. 외부 라이브러리 미사용, 계약 파일/layout_data 스키마 무변경. npm run build 재확인 성공(92 modules). 기능 추가(사용자 요청 + architect 0006_concept_images.sql 계약 반영): 컨셉 관리(Concepts.tsx)에 이미지 업로드 추가 — 기존 인라인 이름수정/1줄 추가폼을 Frames.tsx와 동일한 모달 폼 패턴으로 전환(이름 + 이미지 업로드 필드), concept-images 버킷(공개읽기/super_admin 쓰기, 0006에서 frame-previews와 동일 정책으로 추가됨)에 업로드해 concepts.image_url에 저장, "부스 앱 컨셉 카드 배경으로 쓰이며 3:4 비율 권장"(booth Concept.tsx의 실제 aspect-ratio:3/4 카드 CSS 확인 후 반영) 안내문구 추가, "이미지 제거" 버튼으로 Storage 파일 삭제 후 image_url=null 저장 가능, 목록에 썸네일 컬럼(없으면 기존 thumb--empty 자리표시 재사용) 추가, 생성/수정 모두 기존 concept.create/update 감사로그 패턴에 image_url 포함해 기록. lib/storage.ts의 uploadPublicFile을 PublicBucket 유니온으로 확장(frame-previews/frame-videos/concept-images 재사용)하고 URL로부터 경로를 역산해 삭제하는 removePublicFile 헬퍼 신규 추가. 계약 파일(packages/shared/*, supabase/migrations/*) 무수정 — 0006은 architect가 이미 반영한 상태였고 Concept.image_url 타입도 types.ts에 이미 존재함을 확인 후 사용. npm run build 재확인 성공(92 modules). [긴급 버그 수정] 프레임 신규등록 5건이 preview_image_url=null로 저장된 실서비스 버그 수정 — 근본 원인: Frames.tsx 저장 버튼이 uploadingPreview/uploadingVideo 상태와 무관하게 항상 활성화돼 있어, 이미지 선택 즉시 로컬 objectURL로 미리보기가 뜨는 것을 "완료"로 착각하고 백그라운드 업로드(네트워크, 지연 있음)가 끝나기 전에 저장을 누르면 preview_image_url이 아직 null인 상태로 그대로 insert됨(반면 슬롯 배치/자동인식은 네트워크 대기 없는 로컬 연산이라 정상 저장되어 증상과 일치). 수정: (1) 저장 버튼을 saving||uploadingPreview||uploadingVideo일 때 비활성화하고 "업로드 완료 대기 중..." 표시, handleSubmit 최상단에도 방어적 재검사 추가 — 근본 차단. (2) 업로드 실패 시 기존에는 상단 공용 에러 배너(setError)만 썼는데, handleSubmit 시작 시 setError(null)로 지워질 수 있어 이미지/영상 필드 옆에 개별 에러 상태(previewUploadError/videoUploadError)를 추가해 저장 여부와 무관하게 계속 보이게 함. (3) preview_image_url이 비어있으면 "촬영 앱에서 합성 불가" 경고와 함께 confirm으로 한번 더 확인(저장 자체는 막지 않음). Concepts.tsx도 동일 패턴(uploadingImage 무시)이 있어 동일하게 저장버튼 비활성화 + imageUploadError 개별 표시로 수정(컨셉 이미지는 합성 필수 요소가 아니라 빈 값 confirm은 추가하지 않음). npm run build 재확인 성공(92 modules) |
| infra-setup | infra-agent | project-setup | APPROVED | QA 통과. 0001/0002 미수정(계약 유지, 바이트 단위 재확인) 확인. cleanup-expired-sessions: expires_at<now() OR created_at<cutoff(status<>deleted) 조회 → 3개 세션 버킷 파일 삭제 → row 완전삭제 순서로 스펙 섹션6 3단계 정확히 구현, retention_hours 24~48 클램프 확인. 0003: 세션 버킷 anon read 제거 + get-session-media(service_role, signed URL, raw/selected_photo_urls 미노출)로 signed URL 전용 접근 전환 확인 — 스펙 섹션9 Storage 항목 충족. admins/admin_audit_logs anon 접근 불가 확인. super_admin 세션 삭제/파일목록 확인 기능 보존 확인. 0004 pg_cron 매시간 스케줄 문법 검토 통과(미연결 환경이라 실행 미검증, 문서화됨). 잔여 위험(반려 아님, 기록): (a) sessions 테이블 anon select가 baseline(전체 조회) 유지 — booth insert().select() 패턴 호환 위해 이번 라운드 보류, 완전한 소유권 기반 축소는 sessions.created_by 등 스키마 변경 필요(architect 승인 필요)로 후속 과제 권장. (b) 통계 집계 테이블 미생성은 스펙 섹션6 표현이 "권장"이라 반려 사유 아님 |
| qr-and-result | integrator | booth-flow, print-animation | APPROVED | QA 통과(파이프라인 전체 완료). 4.8: Qr.tsx가 sessionId 없으면 즉시 `/`로 안전 복귀, 있으면 `${VITE_RESULT_BASE_URL}/${sessionId}`로 즉시 QR 값 계산(Generate.tsx가 저장한 qr_url과 동일 규칙) 후 DB의 sessions.qr_url로 교체 — 빈 화면 없이 스펙 취지(qr_url 렌더링) 충족. 25초 자동 복귀는 스펙에 수치 미명시, 합리적 임의값으로 통과. 4.9: apps/result/src/routes/Result.tsx가 get-session-media만 호출(fetchSessionMedia)해 completed/expired/not_found/in_progress/서버·네트워크 오류를 분기, 사진(필수)+타임랩스(있으면)+프레임영상(있으면) 탭 구성, DownloadButton이 Web Share API 우선 후 blob anchor/새창 폴백. App.tsx는 `/:sessionId` 단일 라우트만 존재(내용 미변경 확인). 보안: apps/result 전체에서 raw_photo_urls/selected_photo_urls/service_role 참조 0건(grep 확인), Storage 직접 접근 없이 get-session-media 계약(infra-security-notes.md 3절)대로만 동작. 전체 state 체인 코드 확인: Generate.tsx(sessionId+resultImageUrl blob URL로 /print-animation) → PrintAnimation.tsx(state 그대로 이어받아 /qr로) → Qr.tsx(state.sessionId 사용) 끊김 없음. 계약 파일: supabase/functions/*, apps/admin/routes/* 등 타 담당 파일은 mtime상 이번 작업 이전 것으로 미수정 확인, packages/shared/types.ts는 admin-page 라운드에서 architect가 이미 정정한 내용 그대로(변경 없음). apps/booth·apps/result 모두 `npm run build` 재실행 성공(booth 694 modules/1.2MB 청크 경고는 기존 판정대로 반려 사유 아님, result 84 modules). 6개 task 전원 APPROVED로 파이프라인 완료. [후속 보안 강화 반영] `Qr.tsx`의 불필요한 `sessions.select('qr_url')` 조회(0005 적용 후 anon 차단으로 항상 실패)를 제거하고 로컬 계산값(`${VITE_RESULT_BASE_URL}/${sessionId}`)만 사용하도록 정리(useState 제거, 렌더 분기 단순화). `apps/result/src/lib/getSessionMedia.ts`를 삭제하고 `Result.tsx`의 import를 architect가 공용화한 `@withmini/shared`의 `fetchSessionMedia`/`SessionMediaCompleted`로 교체(동작 변경 없음, 중복 파일 제거). animation-builder가 동시 작업 중인 `PrintAnimation.tsx`는 건드리지 않음. apps/booth·apps/result `npm run build` 재확인 성공(booth 695 modules, result 84 modules) |

## 반려 이력 (CHANGES_REQUESTED 발생 시 여기에 기록)

| task | 반려 횟수 | 마지막 반려 사유 | 반려 시각 |
|---|---|---|---|
| - | - | - | - |

## 사람에게 보고가 필요한 항목

(3회 이상 반려되었거나, 스펙에 없는 애매한 결정이 필요한 경우 여기에 기록)

- [해결됨 — architect] `packages/shared/src/types.ts`의 `Database` 제네릭에 각 Table `Relationships: []`, `public` 스키마에 `Views: { [_ in never]: never }` / `Functions: { [_ in never]: never }`를 추가해 postgrest-js의 GenericSchema 요구사항을 충족시킴(DB 컬럼/타입 자체는 무변경, 순수 타입 레벨 정정이라 스펙 섹션2와 supabase/migrations/*.sql은 그대로 둠). booth-builder는 `apps/booth/src/routes/Generate.tsx`(및 다른 곳)의 `as any` 로컬 우회를 제거해도 된다. admin-builder는 이 정정으로 새로 드러난 `Frames.tsx` 타입 오류 2건을 위 admin-page 행 notes 참고해 수정 필요.
- [스펙에 명시 안 됨, booth-builder 임의 결정 — 필요시 재검토] (1) Canvas 합성 방식: `frames.preview_image_url`을 "사진이 들어갈 자리가 투명한 PNG 템플릿"으로 가정하고 선택 사진들을 슬롯에 먼저 그린 뒤 그 위에 프레임 이미지를 덮어씌우는 방식으로 구현(`packages/shared/src/compositor.ts`). (2) `frames.layout_data.slots`의 좌표값이 0~1이면 프레임 이미지 크기 대비 비율, 아니면 절대 픽셀로 해석하도록 자동 판별. 두 가지 모두 스펙 문서에 명시되지 않아 admin-builder가 프레임 등록 UI를 만들 때 이 규약대로 `layout_data`/`preview_image_url`을 입력하도록 맞춰야 함 — 다르게 가정했다면 조율 필요.
- [integrator 공지] `/generate`는 `sessions.qr_url`을 `${VITE_RESULT_BASE_URL}/${sessionId}`로 채워 넣는다(`apps/booth/.env.example`에 `VITE_RESULT_BASE_URL` 추가, 기본값 `https://withmini.link`). `raw_photo_urls`/`selected_photo_urls`/`result_image_url`은 infra-agent 규약대로 signed URL이 아닌 Storage 오브젝트 경로 문자열이다.
- [해결됨 — architect] 위 "부분 미해결" 항목(interface → type) 후속 조치 완료: `packages/shared/src/types.ts`의 `Admin`/`AdminAuditLog`/`Concept`/`FrameSlot`/`FrameLayoutData`/`Frame`/`Session`/`Setting`과 최상위 `Database`를 모두 `interface` 선언에서 `type` 별칭으로 변경(컬럼 구성·타입 자체는 완전히 동일, 선언 키워드만 정정 — Supabase 공식 codegen 산출물도 항상 type을 쓰는 것과 동일한 이유이며 이번 변경도 DB 스키마/스펙 섹션2에 영향 없음). 적용 후 apps/booth, apps/admin, apps/result 3개 앱 모두 `npm run build` 성공 재확인(booth 693 modules / admin 90 modules, 402KB 번들 / result 35 modules) — integrator가 작업 중인 qr-and-result(Qr.tsx, apps/result) 쪽 실패는 없었고 있었더라도 types.ts 기인이 아님이 구분됨. admin-builder의 `apps/admin/src/lib/db.ts` 로컬 Prettify 보정 클라이언트, booth-builder의 잔여 `as any` 우회는 이제 불필요해졌으니 다음 작업 사이클에 정리 권장(급한 반려 사유는 아님).
- [해결됨 — architect, 파이프라인 완료 후 후속 보안 강화] infra-setup 행에 잔여 위험으로 기록됐던 "`sessions` 테이블 anon select가 baseline(`using(true)`)으로 열려 있어 전체 세션 id를 나열(enumeration)할 수 있는 문제"에 대한 설계 검토·구현을 완료. 실제 코드 전수 조사 결과 anon이 SELECT를 필요로 하는 지점이 전혀 없었음(booth의 insert/update는 `.select()`를 체이닝하지 않고, `Qr.tsx`/`PrintAnimation.tsx`의 read는 로컬 계산값 또는 `get-session-media` Edge Function으로 대체 가능). 검토한 3가지 옵션 — (a) 소유권 컬럼 + 세션별 익명 JWT 발급(스키마 변경 + 신규 Edge Function, 데모 범위 대비 과함) / (b) 스키마 변경 없이 anon SELECT 정책 제거 + booth 코드 소폭 조정(채택) / (c) booth 쓰기까지 Edge Function 경유로 전환(별개의 "쓰기 오남용" 잔여 위험을 다루는 것이라 이번 이슈의 해법은 아님, 향후 과제로만 기록) — 중 (b)를 team-lead 승인 하에 채택. 적용한 것: `supabase/migrations/0005_sessions_select_hardening.sql`(`sessions_select_anon_and_admin` 삭제 후 `sessions_select_admin`을 `is_active_admin()`으로 재생성, 컬럼/테이블 무변경), `docs/withmini_demo_spec.md` 섹션 9에 "`sessions` 테이블 anon SELECT 제한" 항목 추가 + 섹션 10에 "쓰기 경로 Edge Function 전환(향후)" 한 줄 추가, `packages/shared/src/getSessionMedia.ts` 신설(`apps/result/src/lib/getSessionMedia.ts`와 동일 계약, `index.ts`에서 export) — **`apps/result`의 기존 파일과 import 경로는 team-lead 지시대로 그대로 두었고, 이 정리는 integrator에게 배정 예정.** 아직 남은 코드 작업(architect가 아닌 다른 담당자 몫): `Qr.tsx`의 불필요한 `.select('qr_url')` 제거(integrator 배정 예정), `PrintAnimation.tsx` 폴백 분기를 `get-session-media`/`fetchSessionMedia`로 교체(animation-builder 배정 예정). 3개 앱 `npm run build` 재확인 통과(booth 695 modules, admin 90 modules, result 85 modules).
- [계약 변경 — 사용자 요청, 승인됨] 사용자가 직접 요청한 기능 확장: "컨셉마다 사진을 넣고, 부스 컨셉 선택 버튼에 그 사진이 배경으로 깔리고 가운데 컨셉명이 표시"되게 함. team-lead 지시로 architect가 CLAUDE.md 섹션 3 절차대로 계약 변경을 진행: (1) `supabase/migrations/0006_concept_images.sql` — `concepts.image_url text null` 컬럼 추가(nullable, 기존 row/코드 영향 없음), `concept-images` Storage 버킷 신설(공개 읽기 + super_admin만 쓰기, `frame-previews`와 동일한 정책 패턴). (2) `packages/shared/src/types.ts`의 `Concept` 타입에 `image_url: string | null` 반영(Insert는 기존 `Partial<Concept>` 기반이라 별도 수정 불필요). (3) `docs/withmini_demo_spec.md` 섹션 2(concepts 표에 image_url 행 추가), 섹션 3(버킷 구조에 concept-images 추가), 섹션 4.2(컨셉 카드에 image_url 배경 + 컨셉명 중앙 표시, null이면 배경 없이 컨셉명만 표시하는 폴백)에 반영. apps/booth·apps/admin·apps/result 코드는 이번 변경에서 건드리지 않음 — image_url이 nullable이라 기존 코드는 그대로 컴파일/동작함(아래 3개 앱 `npm run build` 재확인으로 검증). booth-builder(컨셉 카드 UI에 배경 이미지 렌더링)와 admin-builder(concept-images 버킷에 이미지 업로드하는 UI, `concepts` 폼에 이미지 업로드 필드 추가)에게 병렬 배정 예정. **0006 마이그레이션은 사용자가 Supabase SQL Editor에서 직접 실행해야 함** — SQL 전문은 team-lead에게 회신.
- [긴급 버그 수정 — team-lead 설계 확정, 실서비스 3번째 RLS 버그] 0005에서 `sessions` anon SELECT를 제거한 여파로, booth가 촬영 완료 시 수행하던 `sessions` 직접 UPDATE(`.eq('id', sessionId)`)가 PostgREST 기준 204(성공처럼 보임)를 반환하면서도 실제로는 0건 갱신되는 버그가 실서비스에서 발견됨(PostgreSQL은 UPDATE의 WHERE절이 대상 행을 읽는 단계에서도 SELECT 정책을 요구하기 때문 — curl로 재현: PATCH 204 후에도 get-session-media가 계속 in_progress 반환, 모든 세션이 영원히 완료되지 않는 상태였음). team-lead가 해결 설계 확정: anon SELECT는 계속 차단하되 "완료 전이"만 허용하는 security definer RPC로 우회. architect가 `supabase/migrations/0007_complete_session_rpc.sql` 작성: (1) `public.complete_session(p_session_id uuid, p_raw_photo_urls jsonb, p_selected_photo_urls jsonb, p_result_image_url text, p_qr_url text) returns void` (booth-builder 병렬 작업 시그니처와 정확히 일치, security definer) — 대상 세션이 존재+`status='in_progress'`+미만료일 때만 갱신, 조건 불충족 시 raise exception, expires_at은 건드리지 않음. `revoke all ... from public; grant execute ... to anon, authenticated, service_role;`. (2) 기존 `sessions_update_anon_and_admin` 정책 drop 후 `sessions_update_admin`(using/check 모두 `is_super_admin()`)으로 교체 — 이 RPC 도입으로 anon 직접 update는 실익이 없어졌고(0005 이후 사실상 전부 no-op였음) 공격면을 줄이는 것. `sessions_insert_anon_and_admin`(세션 생성)은 무변경 유지. `docs/withmini_demo_spec.md` 섹션 9의 "sessions 완료 처리" 행을 이번 사실대로 갱신(기존에 "booth의 UPDATE는 .select() 없이 이루어지므로 무관하게 동작한다"고 적혀 있던 부분이 실제로는 틀렸던 것이었음 — RPC 경유로 정정). apps/*는 수정하지 않음(booth-builder가 별도로 이 RPC를 호출하도록 작업 중, 3개 앱 빌드 회귀 확인은 team-lead가 booth-builder 커밋 후 직접 진행 예정). **0007 마이그레이션은 사용자가 Supabase SQL Editor에서 직접 실행해야 함** — SQL 전문은 team-lead에게 회신.
